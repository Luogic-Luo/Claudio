# 个人 AI 音乐电台「Claudio」项目架构说明文档

## 一、项目概述

Claudio 是一个**个人化 AI 音乐电台项目**，目标是通过读取用户听歌习惯、日程、环境信息，让 AI 像真人 DJ 一样规划音乐、生成语音播报，打造专属的音乐陪伴体验。

---

## 二、整体架构分层

项目分为四大核心层级，从底层数据 / 服务到前端交互，实现端到端的完整闭环：

1. 外部上下文层（数据与第三方服务）
2. 本地大脑层（核心逻辑中枢）
3. 运行时聚合层（提示词与模型交互）
4. 交互表层（用户端播放器）

---

## 三、各层级详细说明

### 1. 外部上下文层（数据与第三方服务）

为项目提供基础数据与能力支持，分为四大模块：

表格


| 模块                              | 核心内容                                                     | 说明                                                                                                                     |
| :-------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------- |
| **用户品味语料（USER/）**         | `taste.md`、`routines.md`、`playLists.json`、`mood-rules.md` | 让 AI 真正 “懂你” 的核心文件，定义听歌偏好、日常习惯、歌单、情绪规则                                                   |
| **Claude Code（BRAIN）**          | `claude -p --output json`                                    | 作为项目的 “大脑”，通过子进程调用 Claude，API用的是mimo-2.5pro                                                         |
| **网易云音乐 API（MUSIC）**       | `search`、`song_url`、`lyric`、`recommend`                   | 提供歌曲检索、获取直链、歌词、推荐等音乐核心能力                                                                         |
| **输入 / 输出服务（VOICE・I/O）** | Fish Audio、飞书、OpenWeather、UPnP                          | 覆盖多场景能力：・Fish Audio：语音合成（TTS）・飞书：读取日程信息・OpenWeather：获取天气数据・UPnP：将音乐推送到家庭功放 |

---

### 2. 本地大脑层（核心逻辑中枢，基于 Node.js）

以 Node.js 搭建的本地服务器作为项目中枢，承担所有业务逻辑调度：

表格


| 模块                   | 核心内容           | 功能说明                                                                                               |
| :--------------------- | :----------------- | :----------------------------------------------------------------------------------------------------- |
| `ROUTER.JS`意图分流    | 指令路由分发       | 简单指令直接处理，音乐相关请求走网易云 API，自然语言请求交给 Claude 处理                               |
| `CONTEXT.JS`提示词组装 | System Prompt 生成 | 整合用户语料（taste/routines）、环境信息、历史数据，构建给 Claude 的系统提示词                         |
| `CLAUDE.JS`大脑适配器  | 子进程交互与解析   | 调用 Claude 子进程，解析其返回的结构化数据（播报内容`say`、歌单`play[]`、理由`reason`、过渡语`segue`） |
| `SCHEDULER.JS`节律调度 | 时间与日程联动     | 实现规则化播放（如 07:00 唤醒音乐、09:00 早间歌单）、小时级情绪检查、日历日程联动                      |
| `TTS.JS`声音管线       | 语音合成与缓存     | 调用 Fish Audio 合成语音，生成 MP3 文件并缓存（`cache/tts/*.mp3`），通过哈希值快速访问                 |
| `STATE.DB`状态与记忆   | 跨重启持久化       | 存储聊天记录、播放历史、播放计划、用户偏好，重启后状态不丢失                                           |

---

### 3. 运行时聚合层（Context Window 与模型交互）

每次用户交互或定时触发时，将多维度数据组装成完整 Prompt，送入模型处理：

1. **Prompt 组装 6 要素**
   * ① 系统提示词：`prompts/dj-persona.ad`（定义 AI DJ 的角色设定与行为规范）
   * ② 用户语料：`user/*.md`（读取用户的品味、习惯文件）
   * ③ 环境注入：`weather · calendar · now`（实时天气、日程、当前时间）
   * ④ 已检索记忆：`state.db · ncm search`（历史状态、相关歌曲检索结果）
   * ⑤ 用户输入 / 工具结果：`/api/chat · ncm search`（用户指令、工具返回的搜索结果）
   * ⑥ 执行轨迹：`scheduler · webhook`（调度器规则、外部回调事件）
2. **模型前向过程**
   * 组装后的 Prompt 送入 Claude，返回结构化数据：`{say, play[], reason, segue}`
   * 后续流程：解析歌单队列 → TTS 合成播报内容 → 通过 WebSocket 推送当前播放信息到前端

---

### 4. 交互表层（用户端播放器）

以 PWA（渐进式 Web 应用）作为用户交互入口，支持 Web App、PWA 或 [localhost](https://localhost) 本地运行：

1. **前端核心功能**
   * 播放器界面：播放控制、进度条、歌单展示
   * 个人中心：Profile、Settings 配置页
   * 预加载优化：Prefetch 10s 音频数据，保证播放流畅度
2. **前后端通信接口**
   * `GET /api/now`：获取当前播放状态
   * `GET /api/taste`：获取用户品味配置
   * `GET /api/plan/today`：获取当日播放计划
   * `WS /stream`：WebSocket 实时推送播放与语音数据

---

## 四、整体数据流转流程

1. **触发条件**：用户主动交互、定时调度、日程联动事件
2. **数据聚合**：Context Window 模块整合用户语料、环境数据、历史状态，生成 Prompt
3. **AI 决策**：Claude 接收 Prompt，返回结构化的播报内容与歌单
4. **执行处理**：
   * 歌单解析：调用网易云 API 获取歌曲直链，构建播放队列
   * 语音合成：调用 Fish Audio 生成播报语音并缓存
   * 状态更新：将播放记录、用户交互写入 `STATE.DB`
5. **输出与推送**：
   * 通过 UPnP 将音乐推送到家庭音响
   * 通过 WebSocket 将当前播放信息、语音数据推送到前端 PWA 播放器

---

## 五、项目特点与优势

* 完全本地化中枢：基于 Node.js 搭建，无需依赖外部云服务中枢，数据隐私可控
* 高度个性化：以用户语料为核心，让 AI 真正适配个人听歌习惯与生活场景
* 模块化可扩展：各层级模块解耦，可轻松替换 API（如更换音乐源、语音合成服务）
* 跨场景适配：支持本地播放、家庭音响推送，适配不同使用场景
